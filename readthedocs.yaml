version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    pre-install:
      - pip install cmdstanpy wheel

python:
  install:
    - method: pip
      path: .
      extra-requirements:
        - development


sphinx:
  configuration: docs/conf.py
